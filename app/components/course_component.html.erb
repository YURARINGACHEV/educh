<div class="mb-2">
  <h2 class="mb-2"><%= @counter %>. <%= @item.title %></h2>
  <h4 class="mb-2"><%=  number_to_currency(@item.price, :unit => "€") %></h4>
  <h4 class="mb-2"><%= @item.location %></h4>
  <%= render(DiscountComponent.new(item: @item)) %>
  <%= EnrolleeComponent.new.total_enrollees(@item) %>
  <p><a href='#'> <%= @notice %> </a></p>
</div>
